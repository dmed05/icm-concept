# {{PROJECT_NAME}} Workspace

## Purpose

{{PROJECT_PURPOSE}}

## Routing

- Place new run-specific source material in `input/`.
- Select the first relevant numbered folder under `stages/`.
- Follow that stage's `CONTEXT.md`.
- Review its `output/` before continuing to the next stage.

## Resources

- `_config/`: stable project-wide rules and constraints
- `shared/`: stable references used by multiple stages
- `input/`: working material supplied for the current run
- `stages/`: numbered workflow stages

## Stage map

{{STAGE_MAP}}
